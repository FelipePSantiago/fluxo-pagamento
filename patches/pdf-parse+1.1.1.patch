diff --git a/node_modules/pdf-parse/index.js b/node_modules/pdf-parse/index.js
index 8c7d4ab..2e54b92 100644
--- a/node_modules/pdf-parse/index.js
+++ b/node_modules/pdf-parse/index.js
@@ -6,18 +6,20 @@ module.exports = Pdf;
 
 let isDebugMode = !module.parent; 
-
-//process.env.AUTO_KENT_DEBUG
-
-
-//for testing purpose
-if (isDebugMode) {
-
-    let PDF_FILE = './test/data/05-versions-space.pdf';
-    let dataBuffer = Fs.readFileSync(PDF_FILE);
-    Pdf(dataBuffer).then(function(data) {
-        Fs.writeFileSync(`${PDF_FILE}.txt`, data.text, {
-            encoding: 'utf8',
-            flag: 'w'
-        });
-        debugger;
-    }).catch(function(err) {
-        debugger;
-    });
-
-}
+// 🔒 Patch aplicado: removemos o bloco de teste que lia ./test/data/*
+// Isso quebrava builds em ambientes como Firebase / Vercel
+// Agora, o pacote só exporta a função Pdf normalmente.
+
+// for testing purpose (removido no patch)
+// if (isDebugMode) {
+//     let PDF_FILE = './test/data/05-versions-space.pdf';
+//     let dataBuffer = Fs.readFileSync(PDF_FILE);
+//     Pdf(dataBuffer).then(function(data) {
+//         Fs.writeFileSync(`${PDF_FILE}.txt`, data.text, {
+//             encoding: 'utf8',
+//             flag: 'w'
+//         });
+//         debugger;
+//     }).catch(function(err) {
+//         debugger;
+//     });
+// }
